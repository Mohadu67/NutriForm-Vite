# Production Environment Variables

# URL de l'API backend en production
# Backend hébergé sur Render : https://nutriform-vite.onrender.com
# Le proxy Netlify (_redirects) redirige /api/* vers Render
# Donc on utilise juste le domaine principal sans /api
VITE_API_URL=https://harmonith.fr

# reCAPTCHA Site Key (même qu'en dev normalement)
VITE_RECAPTCHA_SITE_KEY=**************
